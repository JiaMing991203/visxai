"""VisXAI: Explainable AI for molecular property prediction."""

# Kept in step by hand with `version` in pyproject.toml and `version` in
# package.json (the dev-only frontend toolchain). There is no single-sourcing
# mechanism wired up, so a release means editing all three.
__version__ = "0.2.0"
