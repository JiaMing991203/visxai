# Pinned to the versions the demo notebooks (examples/*.ipynb)
# are currently verified against.
# Mirrors pyproject.toml's [project.dependencies] + the "sequence",
# "graph", and "viz" extras (see pyproject.toml for the >= lower-bound
# version of these same constraints), flattened into one file plus the
# notebook tooling itself for convenience: `pip install -r requirements.txt`
# is enough to open and run every notebook in examples/, no extras syntax
# needed.
#
# xgboost and py3Dmol are deliberately NOT listed here -- no notebook in
# examples/ imports them.
# examples/hf_demo.ipynb requires internet access (downloads a real
# checkpoint on first run) -- everything else in this file runs offline.

# Core (fingerprint/tree path -- examples/tree_demo.ipynb)
rdkit==2025.9.2
shap==0.49.1
numpy==1.26.4
scikit-learn==1.3.0

# Sequence path (examples/sequence_demo.ipynb) -- captum is also used here,
# by IntegratedGradientsExplainer/GradCAMExplainer in gradient_based_sequence.py.
# transformers is used by examples/hf_demo.ipynb (visxai/models/hf_wrapper.py).
torch==2.8.0
captum==0.8.0
transformers==4.57.6

# Graph path (examples/graph_demo.ipynb)
torch_geometric==2.6.1

# Interactive in-notebook dashboard (visxai/visualizers/interactive.py --
# the [viz] extra in pyproject.toml). Optional for the library itself, but
# the test suite imports it, and CI installs from this file.
#
# Must be the 8.x generation to pair with jupyterlab==4.0.11 below: widget
# rendering needs the frontend to match the protocol version the kernel
# emits, and ipywidgets 7 (controls 1.x / jupyterlab_widgets 1.x) renders
# NOTHING on JupyterLab 4 -- silently, with no error, which headless tests
# pass straight through. See pyproject.toml's [viz] comment for the full
# compatibility matrix.
ipywidgets==8.1.8

# anywidget backs the hover-linked MoleculeHoverWidget (Phase 2). Needed in
# CI specifically: the test suite skips every widget-class test
# when it's absent and exercises the ImportError stub instead, so without
# this line CI would silently never test the real widget at all.
anywidget==0.9.21

# Running the test suite -- not needed just to run the notebooks,
# but requirements.txt is also what CI installs from, and CI runs pytest.
pytest==7.4.0

# Running the notebooks themselves
ipykernel==6.28.0
jupyterlab==4.0.11

# visxai itself
-e .
